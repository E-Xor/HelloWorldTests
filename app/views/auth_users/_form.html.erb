<%= form_for(@auth_user) do |f| %>
  <% if @auth_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auth_user.errors.count, "error") %> prohibited this auth_user from being saved:</h2>

      <ul>
      <% @auth_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
  <legend>Enter user details</legend>
  <div>
    <%= f.label :user_name, "Your Name: " %>
    <%= f.text_field :user_name %>
  </div>
  <div>
    <%= f.label :user_password, "Password: " %>
    <%= f.password_field :user_password %>
  </div>
  <div>
    <%= f.label :user_password_confirmation, "Confirm password: " %>
    <%= f.password_field :user_password_confirmation %>
  </div>
  <div>
    <%= f.submit %>
  </div>
  </fieldset>
<% end %>
