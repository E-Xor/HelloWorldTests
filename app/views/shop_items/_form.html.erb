<%= form_for(@shop_item) do |f| %>
  <% if @shop_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_item.errors.count, "error") %> prohibited this shop_item from being saved:</h2>

      <ul>
      <% @shop_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :my_test_scaffold_id %><br />
    <%= f.text_field :my_test_scaffold_id %>
  </div>
  <div class="field">
    <%= f.label :shop_cart_id %><br />
    <%= f.text_field :shop_cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
