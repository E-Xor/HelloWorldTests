if @shop_cart.total_shop_items > 0
  page.replace_html('shop_cart', render(@shop_cart))
  page[:current_shop_item].visual_effect :highlight, :startcolor => "#88ff88", :endcolor => "#114411" if @shop_item.quantity > 0
else
  page[:shop_cart].visual_effect :blind_up
end